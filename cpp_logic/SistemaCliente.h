#ifndef SISTEMA_CLIENTE_H
#define SISTEMA_CLIENTE_H

// --- Lógica de Negocio: Sistema del Cliente ---
// Define las operaciones disponibles para el rol de cliente.
class SistemaCliente {
public:
    SistemaCliente();

    // --- Funciones (a implementar) ---
    // void verMisRutinas(...);
    // void registrarProgreso(...);
};

#endif // SISTEMA_CLIENTE_H
