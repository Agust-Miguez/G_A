package com.example.g_ap.activities;

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;
import com.example.g_ap.R;

// --- Actividad para la Gestión de Pagos ---
// Pantalla para que el administrador pueda registrar, ver
// y gestionar los pagos de las membresías de los clientes.
public class GestionPagosActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        // setContentView(R.layout.activity_gestion_pagos);
    }
}
